# Defaults for ./*compose*.yml:
# https://docs.docker.com/reference/compose-file/interpolation/
# https://docs.docker.com/compose/how-tos/environment-variables/envvars-precedence/
TZ=Europe/Amsterdam
# WARNING: Bind to all network interfaces, usually not a good idea:
# LAN_IP=0.0.0.0
# Generate a random value securely:
#     $ docker run --rm --entrypoint sh "ghcr.io/ignisda/ryot:v10" \
#     -c 'tr -dc "a-zA-Z0-9" < "/dev/urandom" | head -c "20" ; echo'
# SERVER_ADMIN_ACCESS_TOKEN=28ebb3ae554fa9867ba0
